$("p").click(function () {
    $("p").text = "hi"
})


// rn im trying to figure out how to do jquery. which is essential just like javascript, but just simplified.
// the thing with jquery is that there are custom animations, so im trying to do that :)